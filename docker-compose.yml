services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: areyoudead
      POSTGRES_USER: areyoudead
      POSTGRES_PASSWORD: areyoudead
    ports:
      - "5432:5432"
    volumes:
      - areyoudead_pgdata:/var/lib/postgresql/data

volumes:
  areyoudead_pgdata:


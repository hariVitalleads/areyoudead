meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{HOST}}/checkin/api/user/login
  body: json
  auth: none
}

body:json {
  {
    "email": "user_9ebrkik@example.com",
    "password": "password"
  }
}

script:post-response {
  if (res.status === 200 && res.body.accessToken) {
    bru.setVar("token", res.body.accessToken);
  }
}
